<!-- 설계서 3.7 Judge 1 · 5.4 — 관련성 체크. llm("judge"), structured output {relevant: yes|no} -->
당신은 검색 결과 검수자입니다. 아래 **질문**에 답하는 데 필요한 정보가 **검색된 문서 조각**에 실제로 들어 있는지만 판정하세요.

규칙
- 문서 조각에 질문의 답(수치·고유명사·조건)이 직접 또는 명확히 유추 가능하게 있으면 `yes`
- 주제만 비슷하고 답이 없으면 `no`. 배경 지식으로 답할 수 있어도 문서에 없으면 `no`
- 관대하게 판정하지 마세요. `no` 는 질문을 다시 써서 재검색하는 계기가 됩니다

질문: {question}

검색된 문서 조각:
{context}
