<!-- 설계서 2장 "기술 조사" 행 · 5.4 고정 질문 5 — 이 파일은 프롬프트가 아니라 **질문 목록**. 각 질문을 rag_node.ask() 로 기술별 독립 호출.
     {tech} 는 KIVI / InfiniGen 으로 치환. 한 줄 = 질문 하나, 앞의 `key:` 가 TechSummary 필드.
     ` || ` 로 대체 질문을 둘 수 있음 — 앞 질문이 '논문에 근거 없음'이면 다음 질문으로 (호출은 실패했을 때만 추가됨, 전부 retrieval_log 에 남음). -->
overview: {tech}는 무엇을 해결하려는 기술이며 핵심 접근 방향은 무엇인가? (한 문단, 논문의 문제 정의와 제안 요약)
mechanism: {tech}는 KV cache를 구체적으로 어떻게 처리하는가? (핵심 메커니즘 단계별, 알고리즘·자료구조·정책)
numbers: {tech} 논문이 보고한 주요 수치는 무엇인가? (메모리 감소·처리량·속도 향상·정확도 변화 — 실험 모델·환경 포함, 수치 그대로)
limitations: {tech} 논문이 스스로 밝힌 한계·실패 사례·성능이 떨어지는 조건은 무엇인가? || {tech}이 유발하는 추가 오버헤드(사전 계산·메모리·지연)와 정확도가 떨어지는 조건은 무엇인가?
apply_conditions: {tech}를 기존 모델에 적용하려면 재학습·보정 데이터·전용 하드웨어·사전 계산·하이퍼파라미터 중 무엇이 필요한가? 어떤 모델·프레임워크·실험 환경에 적용되었는가? || {tech}를 기존 모델에 적용하려면 어떤 조건이 필요한가? (재학습·보정 데이터·전용 하드웨어·사전 계산·실험 환경 사양)
