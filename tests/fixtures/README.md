# tests/fixtures/

워커 개발을 서로 기다리지 않기 위한 **손으로 쓴 State 예시**. D 가 `state.py` 확정과 함께 채운다.

| 파일 | 용도 |
|---|---|
| `tech_summary.json` | B·C 워커 입력 (KIVI · InfiniGen 각 1건, `[논문 p.N]` 태그 포함) |
| `evals.json` | `market_eval` · `stakeholder_eval` · `domain_eval` 예시 → C synthesis · D report 입력 |
| `synthesis.json` | D report 렌더링 테스트용 |
